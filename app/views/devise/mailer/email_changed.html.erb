<p><%= t("hello", email: @email) %></p>

<% if @resource.try(:unconfirmed_email?) %>
  <p><%= t("password_change", password_change: @resource.unconfirmed_email) %></p>
<% else %>
  <p><%= t("password_change", password_change: @resource.email) %></p>
<% end %>
